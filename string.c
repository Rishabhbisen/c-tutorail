#include <stdio.h>
#include<string.h>

int main()
{
  // char str[] = "rishabh";
  //  printf("%s",str);
 // char str[34];
 // gets(str);
  //printf("%s\n", str);
 // puts(str);
  char str[] ={'a','b','c','d','\0'};
  printf("%s",str);

  return 0;
}